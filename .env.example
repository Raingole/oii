# Memory backend: tencent enables MemoryCore; legacy is the emergency fallback.
# TencentDB Agent Memory settings are stored in data/.config.yaml.
# MEMORY_BACKEND=legacy is available there for emergency rollback.
