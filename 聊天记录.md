# 项目聊天记录 / 工作日志

> 记录时间：2026-08-26
> 仓库：https://github.com/Raingole/oii.git（main 分支）
> 本机项目根目录：`D:\sssssssssssssssssss\serve`
> 服务器部署目录：`/home/oii/项目目录`（更新方式：`git pull origin main` 后重启服务）

---

## 一、项目组成

### 服务端（本仓库 serve/）
- xiaozhi ESP32 语音服务器（Python），WebSocket 地址 `ws://36.212.7.43:8000/xiaozhi/v1/`
- `core/notification_hub.py`：通知队列中心，设备离线时暂存通知（最多 100 条），设备重新上线立即补播
- HTTP 通知接口在 8005 端口，电脑监听器把 QQ/微信新消息推到这里

### 电脑端软件（两个）
| 软件 | 源码（编译前） | 编译产物 | 功能 |
|------|----------------|----------|------|
| 小智消息监听器 | `windows_notification_listener/`（C# WinUI 3） | `xiaozhi-notification-listener/`（publish + MSIX 安装包） | 监听 Windows 系统通知（QQ/微信等），经 WebSocket 推给服务器 |
| 桌面控制被控端 | `desktop_control_listener/`（Python） | `xiaozhi-desktop-control/`（PyInstaller exe） | 配合语音指令远程控制电脑（打开应用等） |
| 桌面监听器（旧版/备用） | `desktop_listener/`（Python） | — | 前身版本，保留 |

> 2026-08-26：两个编译好的软件已从 `D:\software\` 移入仓库根目录；
> 桌面快捷方式已更新指向 `serve\xiaozhi-desktop-control\xiaozhi-desktop-control.exe`。
> `xiaozhi-notification-listener/` 含 1.6GB 自带 dotnet 运行时，只保留本地，已在 .gitignore 中排除。

---

## 二、通知下发机制演进（重要）

### 第一版：独立打断会话（commit adc842b）
服务器收到通知后：
1. 打断当前播放
2. 销毁对话上下文（`Dialogue.clear_context()`，保留人设 system 提示词和 few-shot）
3. TTS 播报通知
4. 播完再销毁上下文（通知内容不进历史、AI 不记得）

保护逻辑：播完后若检测到用户新开了对话（sentence_id 变化），则不销毁。

问题发现：正在回复时硬打断体验不好 → 改进为第二版。

### 第二版：回复末尾追加 + 兜底补播（commit 8a44e4b、410ad07，当前生效）

```
通知到达
├─ 设备空闲 → 立即独立播报（播前/播后销毁上下文）
└─ 对话进行中 → 进入缓冲 pending_notify_texts（最多 10 条）
     ├─ 赶上本轮回复 → 追加到回复末尾一起下发
     │   例：「今晚吃什么」→ 高德地图 MCP 播报周边美食 → 紧跟「新消息提醒」
     └─ 没赶上（音频已在下发中）→ 本轮播放结束瞬间立即补播
         （drain_pending_notifications，多条用「；」连接合并播报，
          复用 speak_txt 流程但不写入对话历史）
```

关键代码位置：
- `core/connection.py`：`notify_text()`、`_destroy_context_after_playback()`、
  `drain_pending_notifications()`、`chat()` 末尾的追加逻辑
- `core/handle/sendAudioHandle.py:310`：`"stop"` 分支触发补播兜底
- `core/utils/dialogue.py`：`clear_context()`

### 关于"强制和 ESP 建立连接"
ESP32 是 WebSocket 客户端，服务器无法主动拨号。实际机制：
ESP 开机保持长连接（随时可下发）；离线时通知进队列；重新上线立即补播
（`_mark_tts_ready` 触发 `http_server.deliver_pending_notifications`）。

---

## 三、踩过的坑

1. **通知文本污染对话**：`speak_txt` 会把文本写入 dialogue 历史（intentHandler.py:382），
   导致通知内容出现在后续对话里 → 通知路径全部绕开 dialogue.put。
2. **通知没播出来**：多为 ESP 当时没连上（日志无"设备已注册通知连接"），进了离线队列，
   下次上线才补播——不是丢消息。
3. **悬空 tool_calls 导致 LLM 报 400**：打断造成的 tool_calls 无响应，
   `_ensure_tool_calls_complete()` 自动补 dummy tool 消息修复。
4. **TTS 未初始化就发通知**：notify_text 里等待 `tts_ready_event`（最多 30s）。

---

## 四、常用操作备忘

```bash
# 服务器更新
cd /home/oii/项目目录 && git pull origin main && 重启服务

# 本地提交推送
git add -A && git commit -m "..." && git push origin main

# 验证通知三个场景
# 1) 闲聊提问（如"今晚吃什么"）→ MCP 结果播完紧跟新消息提醒
# 2) 设备空闲来通知 → 立即播报，之后 AI 不记得通知内容
# 3) AI 正说话时来通知 → 当前话说完紧接着播提醒

# 相关日志关键字
# "对话进行中，通知将追加到本轮回复末尾"
# "回复末尾追加新消息提醒"
# "回复结束后补播新消息提醒"
# "通知播报完成，已销毁对话上下文"
# "设备已注册通知连接"
```

---

## 五、提交记录（近期）

- `410ad07` Drain leftover notifications after reply playback ends（补播兜底）
- `8a44e4b` Append pending notifications to reply tail（回复末尾追加通知）
- `adc842b` Make notifications standalone interrupt sessions(独立打断会话)
- `b14c5f0` Send token in WS URL with handshake watchdog（监听器 WS 握手看门狗）
- `868c2b7` Bypass system proxy for notification WebSocket（绕过系统代理）
- `2e9e3e3` Move HTTP services to port 8005
- `5c9de0e` Add stage 2/3 notification WebSocket client and server hub
